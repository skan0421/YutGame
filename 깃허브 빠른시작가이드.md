# 🚀 빠른 시작 가이드 (5분 완성!)

Git 저장소를 바로 시작할 수 있는 최소한의 명령어만 정리했습니다.

## ✅ 체크리스트

- [ ] Git 설치됨
- [ ] GitHub 계정 있음
- [ ] 현재 위치: `C:\Users\user\Desktop\YutGame`

---

## 📝 1단계: 폴더 이름 변경

**탐색기에서**:
```
yut-console → phase-0-console 으로 이름 변경
```

---

## 📝 2단계: 파일 3개 복사

다운받은 파일들을 `YutGame` 폴더에 복사:

1. **.gitignore** → YutGame 폴더
2. **README.md** → YutGame 폴더  
3. **Git명령어가이드.md** → YutGame 폴더 (나중에 참고용)

---

## 📝 3단계: Git 초기화 (명령 프롬프트)

```bash
# YutGame 폴더로 이동
cd C:\Users\user\Desktop\YutGame

# Git 초기화
git init

# 사용자 정보 설정 (처음 한 번만)
git config --global user.name "본인이름"
git config --global user.email "본인이메일@gmail.com"
```

---

## 📝 4단계: 첫 커밋

```bash
# 모든 파일 추가
git add .

# 커밋
git commit -m "첫 커밋: Phase 0 - 콘솔 버전 완성"

# 버전 태그
git tag -a v0.1.0 -m "Phase 0: 순수 자바 콘솔 게임"
```

---

## 📝 5단계: GitHub 저장소 만들기

1. **https://github.com** 접속
2. **New** 버튼 클릭 (또는 +  아이콘)
3. **Repository name**: `yut-game` 입력
4. **Public** 선택
5. **✅ Add a README 체크 해제** (우리가 이미 만듦)
6. **Create repository** 클릭

---

## 📝 6단계: GitHub에 올리기

```bash
# GitHub 저장소 연결 (본인아이디로 변경!)
git remote add origin https://github.com/본인아이디/yut-game.git

# 업로드
git branch -M main
git push -u origin main
git push origin v0.1.0
```

---

## 🎉 완료!

GitHub에서 본인 저장소를 확인해보세요!

---

## 📂 최종 폴더 구조

```
YutGame/
├── .git/                    ← Git이 자동 생성
├── .gitignore              ← 복사한 파일
├── README.md               ← 복사한 파일
├── Git명령어가이드.md        ← 복사한 파일
└── phase-0-console/        ← 이름 변경한 폴더
    ├── README.md
    └── src/...
```

---

## 🔄 앞으로 작업할 때

### Phase 1 시작할 때

```bash
# phase-1-web 폴더 생성
mkdir phase-1-web

# 작업...

# 커밋
git add .
git commit -m "[Phase-1] 프로젝트 초기 설정"
git push origin main
```

### 매일 작업할 때

```bash
# 파일 수정...

git add .
git commit -m "[Phase-1] 기능: REST API 추가"
git push origin main
```

### Phase 완성했을 때

```bash
git tag -a v1.0.0 -m "Phase 1: 웹 버전 완성"
git push origin main --tags
```

---

## 💡 자주 쓰는 명령어

```bash
# 현재 상태 확인
git status

# 변경 사항 보기
git diff

# 커밋 기록 보기
git log --oneline

# 최신 코드 받기
git pull origin main
```

---

## 🆘 문제가 생기면?

### 커밋 실수
```bash
# 마지막 커밋 취소 (파일은 그대로)
git reset --soft HEAD~1
```

### GitHub 업로드 에러
```bash
# 강제 푸시 (주의!)
git push -f origin main
```

### 처음부터 다시 시작
```bash
# .git 폴더 삭제
rm -rf .git

# 다시 초기화
git init
```

---

## 📚 더 자세한 내용은?

**Git명령어가이드.md** 파일을 참고하세요!

모든 명령어와 자세한 설명이 있습니다.

---

## ✨ 축하합니다!

이제 Git 저장소가 준비되었습니다!

Phase 1 개발을 시작하세요! 🚀
